<div>
  <div class="form-group">
    <%= link_to "Просмотр курса", course_path(@course), class: "btn btn-secondary" %>
  </div>
  <div class="row">
    <div class="col-lg-12">
      <%= simple_form_for([@course, @lesson]) do |f| %>
        <div class="form-inputs">
          <%= f.input :name %>
          <%= f.input :video_link,  hint: 'youtube код видео' %>
          <%= f.input :task_desc, input_html: { rows: 21 },  hint: 'можно вводить Markdown' %>
          <%= f.input :comment_desc %>
          <%= f.input :video_desc, input_html: { rows: 1 }, hint: 'ссылка на видео, в новых не используем' %>
          <%= f.input :reading_desc, input_html: { rows: 1 },  hint: 'можно вводить HTML, в новых не используем' %>
          <%= f.input :hint, input_html: { rows: 1 },  hint: 'можно вводить HTML, в новых не используем' %>
          <%= f.input :notify_open %>
        </div>
        <div class="form-actions">
          <%= f.button :submit, "Сохранить", class: 'btn btn-primary btn-lg' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
