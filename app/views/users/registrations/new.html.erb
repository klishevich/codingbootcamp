<h2>Регистрация</h2>
<div class="alert alert-secondary" role="alert" style="margin-top: 20px;">
  <div>После регистрации Вам будет оправлено письмо для подтверждения email.</div>
</div>
<div class="row">
  <div class="col-lg-6">
    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= f.error_notification %>

        <div class="form-inputs">
          <%= f.input :email, required: true, autofocus: true %>
          <%= f.input :password, required: true, hint: ("#{@minimum_password_length} символов минимум" if @minimum_password_length) %>
          <%= f.input :password_confirmation, required: true %>
          <%= f.input :full_name, required: true %>
          <%= f.input :reg_code, required: true, hint: 'необходимо запросить у ментора' %>
          <%= f.input :comment, required: false %>
          <%= f.hidden_field :code %>
        </div>

        <div class="form-actions">
          <%= f.button :submit, "Зарегистрироваться", class: 'btn btn-primary btn-lg' %>
        </div>
    <% end %>
  </div>
</div>

<%= render "devise/shared/links" %>
